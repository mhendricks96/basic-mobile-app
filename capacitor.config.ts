import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'basic-mobile-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
